

{# including menu here works, just can't get horizontal yet
don't move this to block-system-menu #}

 {{ include ('academica:menu', {
  menu_id: 'test-id',
  layout_type: 'horizontal',
 }) }}

{# how to set menu ID? #}
