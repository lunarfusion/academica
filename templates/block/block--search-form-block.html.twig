
{# <div class="search" {% if attributes is not empty %}{{- attributes|raw -}}{% endif %}>
  {{ title_prefix }}
  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  {{ title_suffix }}

  {{ include ('academica:button', {
      attributes: attributes,
      modifier_class: modifier_class,
      label: label,
      kind: 'button',
      type: 'empty',
      size: 'regular',
      text: 'open search',
      has_icon: true,
      icon_only: true,
      icon: 'search-toggle',
  }) }}

  <div class="search-form-wrapper" id="search-form-drawer" aria-expanded="false">
    {% block content %}
      {{ content }}
    {% endblock %}
  </div>

</div> #}


{# why doesn't this work? renders null #}
{# {{ include ('academica:search') }} #}

{# {% embed 'academica:search' %} #}

{#
{% block content %}
  {% embed 'academica:search' with {
    attributes: create_attribute(),
    search_toggle,
    search_input,
    search_submit
  } only %}
    {% block search_toggle %}
      {{ search_toggle }}
    {% endblock %}
    {% block search_input %}
      {{ children }}
    {% endblock %}
    {% block search_submit %}
      {{ search_submit }}
    {% endblock %}
  {% endembed %}
{% endblock %} #}

<div class="search-form-wrapper" id="search-form-drawer" aria-expanded="false">
  <div{{ attributes }}>
    {{ title_prefix }}
    {% if label %}
    <div>
      <h2{{ title_attributes }}>{{ label }}</h2>
    </div>
    {% endif %}
    {{ title_suffix }}
    {% block content %}
      {{ content }}
    {% endblock %}
  </div>
</div>
